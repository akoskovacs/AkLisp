You can build the modules (from the project's root directory), with the 'make modules' command. After they can loaded with the '(LOAD "modules/*.alm")' command from the interpreter.

NOTE: All files, which have the *.c extension, will be builded as AkLisp loadable modules by the Makefile as *.alm.
