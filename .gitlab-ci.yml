before_script:
    - apt-get update -qq && apt-get -y -qq install build-essential libreadline-dev cmake
    - gcc --version
    - make -v
    - cmake --version

stages:
    - test
    - build

build:
    stage: build
    script:
    - cmake .
    - make

build:
    stage: test
    script:
    - make test
